Python Guidelines:
https://www.python.org/dev/peps/pep-0008/

Chapter 3:

Array/list[-1] = access last element in an arrayd:
print(moto[-1])

title() = format string
append()= add items to an empty array/list:
moto.append(‘honda’)

insert()=inserting elements into an array:
ex. moto.insert(0, ‘ducati’)

del = remove an item inside an array:
del moto[0]

pop() = removed the last item in a list/removed an item inside using indexes:
moto.pop() - remove last item
moto.pop(0) - remove first index

remove() =  remove an item by value:
moto.remove(‘ducati’)

sort() = change the order of the list alphabetically:
moto.sort()
moto.sort(reverse=True)   		# sort in reverse alphabetical

sorted() = display your list in a particular order but doesn’t affect the actual order of the list:
sorted(moto)

reverse() = reverse the list in order:
moto.reverse()

len() = finds the length of the list
len(moto)

Chapter 4: Working With Lists

for loop prints out each element inside an array:
for moto in motos:
	print(moto)

range() = generate a series of numbers:
for value in range(1,5):
	print(valu)

Using range() to make a list:

numbers = list(range(1,6))
print(numbers)

min() = prints the minimum number
max() = prints the maximum
sum() = adds all the numbers insde the list

slicing a list:
moto[0:5] = index 0 to 4
    [:5]  = index 0 to 4
    [1:]  = index 1 to last
    [-3:] = prints out the last three

looping through a slice:

copying a list:
hello_moto = moto[:]    # [:] means copied from first element to last

Defining a Tuple:
moto = ('honda', 'ducatti')


Chapter 5: If Statements


Chapter 6:

del = remove/delete
del moto['ducati']

.items() = keys:value
moto.items()

.keys() = iterate the keys inside a dictionary
moto.keys()

.values() = iterates the values inside a dictionary
moto.values()

sorted() = sorts the list/dictionary in order
sorted(moto.keys())

set() = iterates a list,each item in the set must be unique
set(moto.values())
